<header
{% if section.settings.heading_bg %}
  style="background: {{ section.settings.heading_bg }}"
{% endif %}>
  <h2
  {% if section.settings.heading_color %}
    style="color: {{ section.settings.heading_color }}"
  {% endif %}>{{ section.settings.heading }}</h2>
</header>
<div class="container-fluid ina-outlets">
  <div class="row">
    {% for block in section.blocks %}
      <div class="col-md-6 ina-outlet"
      {% if block.settings.ina_bg %}
        style="background: {{ block.settings.ina_bg }}"
      {% endif %}>
        <h3>{{ block.settings.heading }}</h3>
        <img src="{{ block.settings.ina_image | img_url: 'original' }}" />
        <div class="buttons-container">
          {{ block.settings.hubspot_links }}
          <a href="{{ block.settings.ina_link }}" class="blk-btn">{{ block.settings.ina_button_text }}</a>
          <p>{{ block.settings.content }}</p>
        </div>
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
  {
    "name": "International Outlets",
    "settings": [
      {
        "type": "color",
        "id": "heading_bg",
        "label": "Heading Background Color"
      },
      {
        "type": "color",
        "id": "heading_color",
        "label": "Heading Color"
      },
      {
        "type": "text",
        "id": "heading",
        "label": "Heading"
      }
    ],
    "blocks": [
      {
        "name": "International Outlets",
        "type": "ina_outlet",
        "settings": [
          {
            "type": "color",
            "id": "ina_bg",
            "label": "Outlet Background Color"
          },
          {
            "type": "url",
            "id": "ina_link",
            "label": "Outlet Link"
          },
          {
            "type": "text",
            "id": "ina_button_text",
            "label": "Outlet Link Text"
          },
          {
            "type": "image_picker",
            "id": "ina_image",
            "label": "Outlet Image"
          },
          {
            "type": "text",
            "id": "heading",
            "label": "Outlet Heading"
          },
          {
            "type": "textarea",
            "id": "hubspot_links",
            "label": "HubSpot Links"
          },
          {
            "type": "textarea",
            "id": "content",
            "label": "Outlet Content"
          }
        ]
      }
    ]
  }
{% endschema %}
